{% include 'default/_form.html.twig' with {
    page_title: 'title.change_user_password'|trans,
    form: form,
    submit_label: 'action.save'|trans,
    back_to_list_path: 'user_view',
    back_to_list_params: {'id': user.id}
} %}
